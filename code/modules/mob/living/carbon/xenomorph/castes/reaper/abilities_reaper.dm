/datum/action/xeno_action/activable/night_terror/use_ability(atom/A)
	/*var/mob/living/carbon/xenomorph/reaper/X = owner

	succeed_activate()
	add_cooldown()

	var/list/nearby_living = list()
	for(var/mob/living/L in viewers(WORLD_VIEW, X))
		nearby_living.Add(L)

	for(var/i in GLOB.mob_living_list)
		var/mob/living/L = i
		if(get_dist(L, X) > WORLD_VIEW_NUM)
			continue
		L.disable_lights(TRUE, TRUE, FALSE, FALSE, TRUE, FALSE)*/