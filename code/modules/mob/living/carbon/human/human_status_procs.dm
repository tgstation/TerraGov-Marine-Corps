/mob/living/carbon/human/blind_eyes(amount)
	if(!species.has_organ["eyes"] && stat == CONSCIOUS)
		return
	..()

/mob/living/carbon/human/adjust_blindness(amount)
	if(!species.has_organ["eyes"] && stat == CONSCIOUS)
		return
	..()

/mob/living/carbon/human/set_blindness(amount)
	if(!species.has_organ["eyes"] && stat == CONSCIOUS)
		return
	..()

/mob/living/carbon/human/blur_eyes(amount)
	if(!species.has_organ["eyes"] && stat == CONSCIOUS)
		return
	..()

/mob/living/carbon/human/adjust_blurriness(amount)
	if(!species.has_organ["eyes"] && stat == CONSCIOUS)
		return
	..()

/mob/living/carbon/human/set_blurriness(amount)
	if(!species.has_organ["eyes"] && stat == CONSCIOUS)
		return
	..()

/mob/living/carbon/human/Stun(amount)
	if(HULK in mutations)
		return
	..()

/mob/living/carbon/human/KnockDown(amount)
	if(HULK in mutations)
		return
	..()

/mob/living/carbon/human/KnockOut(amount)
	if(HULK in mutations)
		return
	..()